
<div class="bg-gradient-to-r from-green-400 via-yellow-500 to-indigo-500">
  <div class="bg-gradient-to-r from-yellow-400 via-blue-500 to-pink-500">
    <h1 class=" h-96 text-center p-6 text-7xl">  ArticlesHub </h1>
  </div>
  <ol>
    
  </ol>
    <%
=begin%>
 <% @articles.each do |article| %>
        <h3>
          <a href="<%= article_path(article) %>">
            <%= article.title %>
          </a>
        
        <a href="<%= edit_article_path(article) %>">
          edit
        </a>
        <%= link_to 'delete', article_path(article), method: :delete, data: { confirm: "Are you sure?" } %>
        </h3>
    <% end %>
    <a href= '/articles/new'>create a new article </a> 
  </div> 
<%
=end%>
<a href= '/dashboard'> Articles Dashbaord </a>
<br/>
<a href= '/all_articles'> View The Articles </a>

</div>

    